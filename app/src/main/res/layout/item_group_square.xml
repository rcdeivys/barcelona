<?xml version="1.0" encoding="utf-8"?>
<com.millonarios.MillonariosFC.utils.LinealSquareLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/content_group"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="5dp"
    android:gravity="center"
    android:background="@color/white"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="30dp"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Avatar del Grupo -->
        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/img_group"
            android:layout_width="@dimen/icon_medium"
            android:layout_height="@dimen/icon_medium"
            android:src="@drawable/no_group_perfil" />

        <!-- Nombre del Grupo -->
        <TextView
            android:id="@+id/name_group"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="4dp"
            android:text="@string/group_no_name"
            android:textColor="@color/black"
            android:textSize="@dimen/font_title_size"
            android:textStyle="bold"
            android:maxLines="1"/>

        <!-- Cantidad de miembros -->
        <TextView
            android:id="@+id/cant_members_group"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="4dp"
            android:text="0"
            android:textColor="@color/gray_transparent"
            android:textSize="@dimen/font_caption_size"
            android:maxLines="1"/>

    </LinearLayout>

</com.millonarios.MillonariosFC.utils.LinealSquareLayout>